const receiveMessageController = require("./receiveMessage.controller");
const sendMessageController = require("./sendMessage.controller");

module.exports = {
    sendMessageController,
    receiveMessageController
}